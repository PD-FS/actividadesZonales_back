<%= form_for(activity_place) do |f| %>
  <% if activity_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_place.errors.count, "error") %> prohibited this activity_place from being saved:</h2>

      <ul>
      <% activity_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :place_id %>
    <%= f.text_field :place_id %>
  </div>

  <div class="field">
    <%= f.label :activity_id %>
    <%= f.text_field :activity_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
